---
export interface Props {
  num: number;
  title: string;
}

const { num, title } = Astro.props;
---

<h1 class="flex gap-2 items-baseline">
  <span class="text-gradient font-lato text-3xl sm:text-4xl">{num}.</span>
  <span class="text-heading-primary text-[2.5rem] font-bold">{title.toUpperCase()}</span>
</h1>